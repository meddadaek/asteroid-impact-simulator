# Runtime dependencies -- exactly what the deployed container installs.
#
# scipy is here even though no module in this project imports it: scikit-learn
# pulls it in (sklearn.utils._param_validation imports scipy.sparse), so
# unpickling a model fails without it. Verified by blocking the import and
# watching model loading break.
fastapi==0.141.1
uvicorn==0.52.4
pydantic==2.13.4
numpy==2.2.6
scipy==1.15.3
scikit-learn==1.7.2
joblib==1.5.3
